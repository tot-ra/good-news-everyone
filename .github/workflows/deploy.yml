name: deploy
on:
  push:
    branches: main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      # restart.sh already does: shallow pull of main, npm ci, vite build, publish dist/ -> public/
      - name: pull and rebuild
        run: /www/good.kurapov.ee/restart.sh
